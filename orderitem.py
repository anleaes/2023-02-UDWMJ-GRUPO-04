class orderitem:

 def __init__(self, quantity, unitary_price, order, product):
   self.quantity = quantity
   self.unitary_price   
   self. order = order
   self. product = product

 def lista_pedidos (self):
     print(f"quantidade = {self.quantity}. ")
     print(f"preço unitário = {self.unitary_price}. ")
     print(f"pedidos = {self.pedidos}. ")
     print(f"produtos = {self.product}. ") 
